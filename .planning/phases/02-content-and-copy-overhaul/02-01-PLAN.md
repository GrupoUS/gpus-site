---
phase: 02-content-and-copy-overhaul
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/products/curso-auriculo.json
  - src/content/products/mentoria-black-neon.json
  - src/content/products/comunidade-us.json
  - src/content/products/trintae3.json
  - src/content/products/otb.json
  - src/content/products/neon-dash.json
  - src/content/products/na-mesa-certa.json
  - src/content/team/sacha.json
  - src/content/team/mauricio.json
  - src/content/team/raquel.json
  - src/components/layout/Header.astro
  - src/components/layout/Footer.astro
  - src/components/home/Hero.astro
  - src/components/home/StatsSection.astro
  - src/components/home/AboutPreview.astro
  - src/components/home/CTASection.astro
  - src/components/about/Mission.astro
  - src/components/about/Values.astro
autonomous: true
requirements:
  - COPY-01

must_haves:
  truths:
    - "No product or team JSON contains unaccented Portuguese words that should be accented (e.g., 'Saude', 'Estetica', 'Avancada', 'tecnica', 'pratica', 'gestao', 'formacao')"
    - "No hardcoded text in Astro layout/home/about components contains missing Portuguese accents"
    - "The build passes with zero Zod schema violations after the accent fixes"
  artifacts:
    - path: "src/content/products/curso-auriculo.json"
      provides: "Accent-verified product JSON"
    - path: "src/content/products/mentoria-black-neon.json"
      provides: "Accent-verified product JSON"
    - path: "src/content/team/sacha.json"
      provides: "Accent-verified team JSON"
  key_links:
    - from: "src/content/products/*.json"
      to: "src/content.config.ts"
      via: "Zod schema validation at build time"
      pattern: "bun run build"
---

<objective>
Systematically audit and fix Portuguese accent/typography issues across all 7 product JSONs, all 3 in-scope team JSONs (out-of-scope team JSONs audited but not modified unless obvious errors found), and hardcoded Portuguese text in Astro components (Header, Footer, home, about).

Purpose: Establish a clean text foundation before copy rewrites in Plan 02-02. Accent errors in content that runs through SEO meta or public-facing headings undermine brand credibility.

Output: All JSON and component files pass a grep-based accent audit. Build continues to pass (Zod only validates structure, but the build gate confirms no file was corrupted).
</objective>

<execution_context>
@/home/mauricio/gpus/.claude/get-shit-done/workflows/execute-plan.md
@/home/mauricio/gpus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/mauricio/gpus/.planning/PROJECT.md
@/home/mauricio/gpus/.planning/ROADMAP.md
@/home/mauricio/gpus/AGENTS.md
@/home/mauricio/gpus/.planning/phases/02-content-and-copy-overhaul/02-CONTEXT.md

Key constraints from CONTEXT.md:
- D-12: Systematic audit. Fix unaccented Portuguese (Saude→Saúde, Estetica→Estética, Avancada→Avançada, tecnica→técnica, pratica→prática, gestao→gestão, formacao→formação, educacao→educação).
- D-13: Typographic quotes — fix ONLY if clearly wrong (e.g., open quote used as close quote). No forced site-wide transformation of straight quotes to curly quotes.
- Do NOT change any CTA URLs, `externalSiteUrl`, `cta.whatsappMessage`, or JSON structure — accent audit touches text copy fields only.
- Out-of-scope team JSONs (andressa, ariane, bruno, erika, jessica, joao-vitor, lucas, renata, riller, roberta): audit only; fix obvious accent errors if found but do NOT enrich or rewrite bios.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix accent issues in all 7 product JSONs</name>
  <files>
    src/content/products/curso-auriculo.json
    src/content/products/mentoria-black-neon.json
    src/content/products/comunidade-us.json
    src/content/products/trintae3.json
    src/content/products/otb.json
    src/content/products/neon-dash.json
    src/content/products/na-mesa-certa.json
  </files>
  <read_first>
    - src/content/products/curso-auriculo.json (current state before any edit)
    - src/content/products/mentoria-black-neon.json
    - src/content/products/comunidade-us.json
    - src/content/products/trintae3.json
    - src/content/products/otb.json
    - src/content/products/neon-dash.json
    - src/content/products/na-mesa-certa.json
    - src/content.config.ts (Zod schema — confirm field names match before touching any JSON)
    - AGENTS.md (project constraints — no emojis, no CTA URL changes)
  </read_first>
  <action>
    Run a grep audit on all 7 product JSON files to find unaccented Portuguese words. For each file that has hits, open the file and fix every unaccented instance. Do NOT touch any field that isn't a text copy field — specifically, never modify: `slug`, `type`, `icon`, `image`, `order`, `externalSiteUrl`, `cta.url`, `cta.type`, `cta.whatsappMessage`.

    Accent mapping to apply:
    - "Saude" → "Saúde" (e.g., "Saude Estetica", "saude estetica")
    - "Estetica" or "estetica" → "Estética" or "estética"
    - "Avancada" or "avancada" → "Avançada" or "avançada"
    - "tecnica" → "técnica" (noun/adjective context; NOT inside URLs or slug fields)
    - "pratica" → "prática"
    - "gestao" → "gestão"
    - "formacao" → "formação"
    - "educacao" → "educação"
    - "posicao" → "posição"
    - "inscricao" → "inscrição"
    - "publico" → "público" (noun/adjective meaning audience; watch context)
    - "unico" / "unica" → "único" / "única"
    - "otimo" → "ótimo"

    Grep commands to run (run these FIRST, fix only what is found):
    ```
    grep -rn --include="*.json" -i "saude\|estetica\|avancada\|gestao\|formacao\|educacao\|inscricao" src/content/products/
    grep -rn --include="*.json" "tecnica\|pratica\|posicao\|unico\|unica\|otimo" src/content/products/
    ```

    After identifying hits: open each affected file, fix the specific strings, preserve all other content unchanged.

    SAFETY: After fixing, verify the JSON is still valid by confirming it parses correctly (executor should run a quick check or use the build gate).

    NOTE from research: Current product JSONs appear largely well-accented already. Most fixes will likely be zero or minimal. Do not invent changes — only fix genuine missing accents found by grep.
  </action>
  <verify>
    <automated>
      grep -rn --include="*.json" -i "saude[^d]\|estetica[^l]\|avancada\|gestao\|formacao\|educacao\|inscricao" src/content/products/ | grep -v '"url"' | grep -v '"slug"'
      # Expected: zero matches (or document any intentional exceptions)
      bun run build
      # Expected: exits 0, Zod schema validation passes
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -rni "saude\|estetica\|avancada\|gestao\|formacao\|educacao" src/content/products/` returns zero matches outside of URL fields and slug fields
    - `bun run build` exits 0 with no Zod schema errors
    - No `cta.url`, `externalSiteUrl`, `slug`, `icon`, `image`, or `cta.whatsappMessage` values were changed (verify with git diff)
  </acceptance_criteria>
  <done>All 7 product JSON files pass accent grep audit. Build succeeds. No structural or URL fields modified.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix accent issues in team JSONs and Astro components</name>
  <files>
    src/content/team/sacha.json
    src/content/team/mauricio.json
    src/content/team/raquel.json
    src/components/layout/Header.astro
    src/components/layout/Footer.astro
    src/components/home/Hero.astro
    src/components/home/StatsSection.astro
    src/components/home/AboutPreview.astro
    src/components/home/CTASection.astro
    src/components/about/Mission.astro
    src/components/about/Values.astro
  </files>
  <read_first>
    - src/content/team/sacha.json
    - src/content/team/mauricio.json
    - src/content/team/raquel.json
    - src/components/layout/Header.astro
    - src/components/layout/Footer.astro
    - src/components/home/Hero.astro
    - src/components/home/StatsSection.astro
    - src/components/home/AboutPreview.astro
    - src/components/home/CTASection.astro
    - src/components/about/Mission.astro
    - src/components/about/Values.astro
    - AGENTS.md (constraints — no hardcoded content in components per Content Collections policy; accent fixes in .astro are scope-limited to fixing existing hardcoded text, not adding new hardcoded content)
  </read_first>
  <action>
    Run accent audit on the 3 in-scope team JSONs and all listed Astro components. Apply the same accent mapping from Task 1.

    Grep commands to run first:
    ```
    grep -rn -i "saude\|estetica\|avancada\|gestao\|formacao\|educacao\|tecnica\|pratica" src/content/team/sacha.json src/content/team/mauricio.json src/content/team/raquel.json

    grep -rn -i "saude\|estetica\|avancada\|gestao\|formacao\|educacao\|tecnica\|pratica" src/components/layout/Header.astro src/components/layout/Footer.astro src/components/home/ src/components/about/
    ```

    For team JSONs: fix any missing accents in `name`, `role`, `bio` fields only. Do NOT change `photo`, `order`, `social` URLs.

    For Astro components: fix only visible text strings — label text, aria-label strings, heading text, paragraph copy. Do NOT touch Tailwind class names, import paths, CSS values, JavaScript, or TypeScript.

    ALSO: Run the out-of-scope team JSON audit (read-only intent, fix only blatant errors):
    ```
    grep -rn -i "saude\|estetica\|avancada" src/content/team/ | grep -v "sacha\|mauricio\|raquel"
    ```
    Fix obvious accent errors in out-of-scope team JSONs if found (do NOT enrich or rewrite their bios — accent-only fixes).

    D-13 applies: typographic quote fix only if clearly wrong. Do not do a site-wide straight-to-curly quote transformation.
  </action>
  <verify>
    <automated>
      grep -rn -i "saude[^d]\|estetica[^l]\|avancada\|gestao\|formacao\|educacao" src/content/team/ src/components/layout/ src/components/home/ src/components/about/ | grep -v ".url\|href\|import"
      # Expected: zero matches
      bunx astro check
      # Expected: exits 0, no TypeScript errors
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -rni "saude\|estetica\|avancada\|gestao\|formacao\|educacao" src/content/team/` returns zero matches in text content fields (name, role, bio)
    - `grep -rni "saude\|estetica\|avancada" src/components/layout/ src/components/home/ src/components/about/` returns zero matches in string literals (excluding href/import/class values)
    - `bunx astro check` exits 0 with no new TypeScript or component errors
    - No social URLs, photo paths, import statements, class names, or href attributes were modified
  </acceptance_criteria>
  <done>All in-scope team JSONs and listed Astro components pass accent audit. Astro check passes. No structural or URL fields modified.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rni "saude[^d]\|estetica[^l]\|avancada\|gestao\|formacao\|educacao\|inscricao" src/content/products/ src/content/team/` — zero matches outside URL/slug/icon fields
2. `bun run lint && bunx astro check && bun run build` — all three exit 0
3. `git diff --stat` — only text copy fields changed; no URL, slug, icon, or image fields in the diff
</verification>

<success_criteria>
- All 7 product JSONs, all 13 team JSONs, and all listed Astro components are accent-clean
- Zero instances of: "Saude", "Estetica", "Avancada", "gestao", "formacao", "educacao" appearing as unaccented Portuguese in copy fields
- Full build pipeline passes: `bun run lint && bunx astro check && bun run build`
- No CTA URLs, slugs, or image paths were changed
</success_criteria>

<output>
After completion, create `/home/mauricio/gpus/.planning/phases/02-content-and-copy-overhaul/02-01-SUMMARY.md`
</output>
