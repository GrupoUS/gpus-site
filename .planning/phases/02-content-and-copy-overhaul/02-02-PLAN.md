---
phase: 02-content-and-copy-overhaul
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/products/curso-auriculo.json
  - src/content/products/mentoria-black-neon.json
  - src/content/products/comunidade-us.json
  - src/content/products/trintae3.json
  - src/content/products/otb.json
  - src/content/products/neon-dash.json
  - src/content/products/na-mesa-certa.json
autonomous: true
requirements:
  - COPY-02

must_haves:
  truths:
    - "mentoria-black-neon description is ≥120 chars and contains no CTA language ('Fale no WhatsApp', 'botão principal')"
    - "comunidade-us tagline includes the brand lema 'Se sozinho você já brilha, juntos iluminamos' or a compelling reformulation"
    - "All 7 product JSONs pass Zod schema validation (painPoints≥3, pillars=3, benefits≥4, differentials≥2, faqs≥3, testimonials≥2)"
    - "No cta.url, externalSiteUrl, or cta.whatsappMessage values were changed"
    - "No emojis appear in any modified JSON field"
    - "curso-auriculo copy is preserved (only tightened if weak fields are found)"
  artifacts:
    - path: "src/content/products/mentoria-black-neon.json"
      provides: "Updated description field ≥120 chars, SEO-clean"
      contains: "description"
    - path: "src/content/products/comunidade-us.json"
      provides: "Elevated tagline with brand lema"
      contains: "tagline"
    - path: "src/content/products/trintae3.json"
      provides: "Audited product JSON"
    - path: "src/content/products/otb.json"
      provides: "Audited product JSON (card-visible fields confirmed strong)"
  key_links:
    - from: "src/content/products/mentoria-black-neon.json .description"
      to: "src/pages/mentoria-black-neon.astro description={d.description}"
      via: "Astro getCollection pattern"
      pattern: "description.*d\\.description"
    - from: "src/content/products/curso-auriculo.json .description"
      to: "src/pages/curso-auriculo.astro description={d.description}"
      via: "Astro getCollection pattern"
      pattern: "description.*d\\.description"
---

<objective>
Audit-first product copy review and targeted rewrite for all 7 product JSONs. Apply D-01/D-02/D-03: read each product, assess current quality, and rewrite only where needed. Strong products (curso-auriculo, otb, na-mesa-certa) are preserved or tightened. Weak fields get targeted upgrades. One confirmed gap must be closed: mentoria-black-neon description (87 chars, contains CTA language — must become ≥120 clean SEO copy).

Purpose: Align all product copy with brand voice and fix measurable quality gaps before the SEO plan (02-03) runs. The mentoria-black-neon description fix is critical because it doubles as the page's SEO meta description via `{d.description}`.

Output: All 7 product JSONs with copy that is brand-aligned, accent-correct, and schema-valid. At minimum: mentoria-black-neon description ≥120 chars, comunidade-us tagline elevated, trintae3 deep fields audited for placeholder language.
</objective>

<execution_context>
@/home/mauricio/gpus/.claude/get-shit-done/workflows/execute-plan.md
@/home/mauricio/gpus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/mauricio/gpus/.planning/PROJECT.md
@/home/mauricio/gpus/.planning/ROADMAP.md
@/home/mauricio/gpus/AGENTS.md
@/home/mauricio/gpus/.planning/phases/02-content-and-copy-overhaul/02-CONTEXT.md
@/home/mauricio/gpus/.planning/phases/02-content-and-copy-overhaul/02-RESEARCH.md

<interfaces>
<!-- Critical patterns the executor MUST respect. Extracted from codebase. -->

From src/pages/curso-auriculo.astro and src/pages/mentoria-black-neon.astro:
```astro
// Pattern A — JSON-powered meta (both landing pages):
const d = product.data;
<Layout description={d.description}>
// Editing the JSON .description field IS the SEO meta change. One edit, two effects.
```

From src/content.config.ts — Zod schema minimums (MUST NOT break):
```typescript
painPoints: z.array(...).min(3),          // min 3 items
pillars:    z.array(...).length(3),        // EXACTLY 3 — not min, not max
benefits:   z.array(z.string()).min(4),    // min 4 string items
differentials: z.array(...).min(2),       // min 2 items
faqs:       z.array(...).min(3),          // min 3 items
testimonials:  z.array(...).min(2),       // min 2 items
cta.type:   z.literal("primary")          // must stay "primary"
```

WhatsApp message rule (AGENTS.md):
```json
// CORRECT — "Olá, Laura!" prefix always
"whatsappMessage": "Olá, Laura! [message text]"
// WRONG — never inline wa.me URLs in content fields
```

From src/lib/whatsapp.ts — WhatsApp number source of truth:
// SDR Laura: +55 62 9470-5081 (556294705081)
// CTA URLs that already contain wa.me links: NEVER change these
</interfaces>

Brand voice (D-05):
- Tone: professional, acolhedor, inspirador, firme. Always "Nós".
- Key phrases to weave in naturally: "Nós iluminamos", "Clareza é a nova gentileza", "Olhar de dono", "Excelência com entrega real"
- No emojis anywhere in content fields (AGENTS.md hard constraint)

Source conflict resolutions (RESEARCH.md):
- OTB location: Dubai 2026 (NOT Boston/Harvard — follow the JSON, not the Manual)
- TRINTAE3 duration: 18 months (NOT "6 meses" — follow the JSON FAQ, not the commercial promise)
- Investment figures: NEVER publish prices; direct to WhatsApp/checkout
- Facts: only use numbers/claims present in gpus-company-info.md or drasacha-content.md

Redirect-only products (D-06):
- comunidade-us, trintae3, neon-dash, na-mesa-certa, otb have externalSiteUrl
- Their JSON only renders in ProductsGrid cards: card-visible = name, tagline, description, icon, image
- Deep field rewrites (painPoints, pillars, faqs) are Claude's discretion — only if sources have clear content
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and rewrite redirect-only product card fields</name>
  <files>
    src/content/products/comunidade-us.json
    src/content/products/trintae3.json
    src/content/products/otb.json
    src/content/products/neon-dash.json
    src/content/products/na-mesa-certa.json
  </files>
  <read_first>
    - src/content/products/comunidade-us.json (current state)
    - src/content/products/trintae3.json
    - src/content/products/otb.json
    - src/content/products/neon-dash.json
    - src/content/products/na-mesa-certa.json
    - docs/plans/aprimoramento/gpus-company-info.md (brand source — product descriptions, lema)
    - .planning/research/drasacha-content.md (secondary source — product framing)
    - AGENTS.md (hard constraints)
  </read_first>
  <action>
    Audit each of the 5 redirect-only products. Focus is on card-visible fields: `tagline` and `description`. Apply D-01: assess current quality before writing anything.

    **Per-product instructions:**

    **comunidade-us** (MEDIUM quality, needs tagline elevation):
    - Current tagline: "Nunca pare de evoluir. Aulas ao vivo, networking exclusivo e suporte contínuo para quem quer se manter na vanguarda da Saúde Estética."
    - Current description: "Comunidade de aprendizado contínuo com conteúdo exclusivo, lives semanais e networking entre profissionais da saúde estética." (98 chars — short)
    - ACTION: Replace tagline with the brand lema from gpus-company-info.md: "Se sozinho você já brilha, juntos iluminamos." followed by a short benefit clause. Example: "Se sozinho você já brilha, juntos iluminamos — educação contínua, networking real e aulas ao vivo para quem não para de crescer."
    - ACTION: Expand description to ≥120 chars. Example target: "Comunidade de educação contínua com curadoria da Dra. Sacha: aulas ao vivo mensais, trilhas gravadas, app exclusivo e networking ativo entre profissionais da Saúde Estética Avançada." (Check length ≥120 chars before saving.)
    - Preserve all other fields unchanged.

    **trintae3** (MEDIUM quality, card fields adequate):
    - Current tagline: "A única Pós-graduação reconhecida pelo MEC que também é Mentoria. Técnica avançada, gestão de negócios e acompanhamento real para você se tornar referência." — STRONG, preserve.
    - Current description: "Pós-graduação reconhecida pelo MEC com mentoria prática: técnica avançada, gestão de clínica e acompanhamento para enfermeiros, biomédicos, farmacêuticos e fisioterapeutas que buscam referência em estética." (199 chars) — GOOD, passes ≥120.
    - ACTION (D-03): Check `benefits[2]` — "Acesso a protocolos validados em mais de 5.000 procedimentos" — round number; this is borderline. Leave unchanged unless a more accurate number is available from sources. Do NOT invent new numbers.
    - Check `differentials[2].description` — "Desenvolvida por quem já formou mais de 5.000 profissionais" — same concern. Leave as-is; this is source-consistent.
    - If no clear improvements found: document in task that trintae3 card fields were audited and no changes made.

    **otb** (HIGH quality, card fields excellent):
    - Current tagline (154 chars description): both fields strong per research.
    - ACTION: Audit-only. Do NOT rewrite. Confirm `description` is ≥120 chars (it is: 154 chars). Confirm tagline is clear. If no issues found, document "no changes — quality confirmed."

    **neon-dash** (MEDIUM quality, tagline functional):
    - Current tagline: "Pare de planejar e comece a executar. Sprints de 90 dias com métricas claras para transformar sua clínica em um negócio previsível." — functional but lacks brand phrases.
    - Current description: 157 chars — passes ≥120.
    - ACTION (D-06, Claude's discretion): Optional tighten. Consider adding brand energy: "Pare de planejar e comece a executar. Sprints de 90 dias com métricas, cadência e o olhar de dono que transforma clínica em negócio previsível." Only make this change if the result is clearly stronger and stays under ~160 chars.

    **na-mesa-certa** (HIGH quality, excellent copy):
    - ACTION: Audit-only. Confirm description ≥120 chars (it is: 146 chars). No changes needed. Document "no changes — quality confirmed."

    CRITICAL REMINDERS for all 5 files:
    - NEVER change: `slug`, `externalSiteUrl`, `cta.url`, `cta.whatsappMessage`, `cta.type`, `icon`, `image`, `order`
    - NEVER add emojis
    - NEVER publish investment amounts or prices (not applicable to card fields but keep in mind for deep fields if touched)
    - Preserve Zod schema minimums if any deep fields are touched (painPoints≥3, pillars=3, benefits≥4, etc.)
  </action>
  <verify>
    <automated>
      bunx astro check
      # Expected: exits 0
      bun run build
      # Expected: exits 0, Zod validation passes for all 5 files
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/content/products/comunidade-us.json` `.tagline` contains "Se sozinho você já brilha" (the brand lema, per D-06 card field focus)
    - `src/content/products/comunidade-us.json` `.description` is ≥120 chars: `node -e "const d = require('./src/content/products/comunidade-us.json'); console.log(d.description.length)"` outputs ≥120
    - No `cta.url`, `externalSiteUrl`, `slug`, `icon`, `image`, `cta.whatsappMessage` changed across all 5 files (verify with git diff)
    - `bun run build` exits 0 with no Zod errors
    - No emoji characters in any modified JSON field: `grep -P "[\x{1F000}-\x{1FFFF}]" src/content/products/*.json` returns zero matches
  </acceptance_criteria>
  <done>All 5 redirect-only product card fields audited. comunidade-us tagline elevated with brand lema. comunidade-us description ≥120 chars. otb and na-mesa-certa confirmed as-is. trintae3 and neon-dash tightened where clear improvements existed. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix landing product copy — curso-auriculo and mentoria-black-neon</name>
  <files>
    src/content/products/curso-auriculo.json
    src/content/products/mentoria-black-neon.json
  </files>
  <read_first>
    - src/content/products/curso-auriculo.json (current state — recently /evolve'd, HIGH quality)
    - src/content/products/mentoria-black-neon.json (current state — HIGH quality, known gap: description 87 chars with CTA language)
    - docs/plans/aprimoramento/gpus-company-info.md (brand voice, product positioning)
    - .planning/research/drasacha-content.md (secondary framing reference)
    - AGENTS.md (constraints)
    - src/content.config.ts (Zod schema — verify minimums after any change)
  </read_first>
  <action>
    These are the two products with full local .astro landing pages. Their `description` field serves dual purpose: product copy AND SEO meta description via `description={d.description}` in the page frontmatter. Apply D-01 (audit-first).

    **curso-auriculo** — AUDIT-ONLY approach (D-03):
    Current description (207 chars): "Imersão presencial de 3 dias: Curso de Aurículo com Técnica de Perfuração Auricular para ampliar o portfólio com segurança na aplicação. Investimento, lote e parcelamento você confere no checkout (botão principal)."

    Known issue: description contains "Investimento, lote e parcelamento você confere no checkout (botão principal)" — this is CTA language that reads awkwardly as an SEO meta tag. Per D-03, tighten this specific field.

    TARGET description (must be ≥120 chars, no CTA language, works as SEO meta):
    Replace with: "Imersão presencial de 3 dias em auriculoterapia e técnica de perfuração auricular para profissionais da saúde e estética — aprenda a aplicar, diversifique o portfólio e atenda com mais segurança desde a primeira semana."
    (Length: ~215 chars — verify before saving. Remove the checkout reference; the checkout CTA is in the `cta` object, not in the description.)

    All other curso-auriculo fields: READ and assess. Only change if a specific weak field is identified (e.g., a benefit that is redundant, a FAQ answer with awkward phrasing). Do NOT do a full rewrite. The copy is strong — preserve it.

    **mentoria-black-neon** — TARGETED FIX REQUIRED (D-03):
    Current description (87 chars, CTA language): "Mentoria Black NEON (6 meses): gestão, vendas e posicionamento premium para clínicas de saúde estética. Menos dependência do fundador. Fale no WhatsApp sobre vagas e próximo ciclo."

    CONFIRMED GAP: 87 chars is below the ≥120 char requirement AND "Fale no WhatsApp sobre vagas e próximo ciclo" is CTA language that reads poorly as an SEO snippet.

    TARGET description (≥120 chars, no CTA language, works as both product summary AND SEO meta):
    Replace `description` with: "Mentoria Black NEON — 6 meses de acompanhamento ativo em gestão, posicionamento premium e vendas para donos de clínicas de Saúde Estética que querem escalar com estratégia e menos dependência do fundador."
    (Length: ~200 chars — verify before saving. No WhatsApp reference, no urgency/CTA language.)

    All other mentoria-black-neon fields: READ and assess field by field. The product has rich copy (story, bio, bonus, deliverables sections). Only change fields where a specific weakness is found. The `bio.paragraphs[0]` contains "13 anos como empresária" — consistent with sources; keep. The `cta.url` is a wa.me link — DO NOT CHANGE IT.

    AFTER CHANGES: Run the Zod schema count check mentally:
    - mentoria-black-neon: painPoints=6 ✓, pillars=3 ✓, benefits=10 ✓, differentials=3 ✓, faqs=5 ✓, testimonials=2 ✓
    - curso-auriculo: painPoints=3 ✓, pillars=3 ✓, benefits=5 ✓, differentials=3 ✓, faqs=7 ✓, testimonials=2 ✓

    CRITICAL: NEVER change `cta.url` for either product:
    - curso-auriculo: `"url": "https://pay.kiwify.com.br/kMXdriO"` — KEEP
    - mentoria-black-neon: `"url": "https://wa.me/556294705081?text=..."` — KEEP (already is a WhatsApp CTA)
  </action>
  <verify>
    <automated>
      node -e "const d = require('./src/content/products/mentoria-black-neon.json'); console.log('mentoria desc length:', d.description.length, '>=120:', d.description.length >= 120)"
      # Expected: length ≥120 and true

      node -e "const d = require('./src/content/products/curso-auriculo.json'); console.log('auriculo desc length:', d.description.length, '>=120:', d.description.length >= 120)"
      # Expected: length ≥120 and true

      grep -n "Fale no WhatsApp\|botão principal\|checkout (bot" src/content/products/mentoria-black-neon.json src/content/products/curso-auriculo.json
      # Expected: zero matches in description field (these phrases OK in other fields if present)

      bunx astro check && bun run build
      # Expected: both exit 0
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/content/products/mentoria-black-neon.json` `.description` is ≥120 chars AND does not contain "Fale no WhatsApp" in the description field
    - `src/content/products/curso-auriculo.json` `.description` is ≥120 chars AND does not contain "(botão principal)" in the description field
    - `src/content/products/mentoria-black-neon.json` `.cta.url` still equals `https://wa.me/556294705081?text=Ol%C3%A1%2C%20Laura!...` (unchanged)
    - `src/content/products/curso-auriculo.json` `.cta.url` still equals `https://pay.kiwify.com.br/kMXdriO` (unchanged)
    - `bunx astro check && bun run build` exits 0
    - No emojis in any modified field
  </acceptance_criteria>
  <done>mentoria-black-neon description ≥120 chars, SEO-clean, no CTA language. curso-auriculo description ≥120 chars, SEO-clean. Both products' cta.url unchanged. Full build pipeline passes.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `bun run lint && bunx astro check && bun run build` — all three exit 0
2. `node -e "const d = require('./src/content/products/mentoria-black-neon.json'); console.log(d.description.length)"` — outputs ≥120
3. `node -e "const d = require('./src/content/products/comunidade-us.json'); console.log(d.tagline)"` — output contains "Se sozinho você já brilha"
4. `grep -n "Fale no WhatsApp\|botão principal" src/content/products/mentoria-black-neon.json` — zero matches in description field
5. `git diff --name-only` — only product JSON files in diff, no .astro files, no other files
</verification>

<success_criteria>
- All 7 product JSONs pass Zod schema validation (confirmed via build)
- mentoria-black-neon.json description is ≥120 chars and SEO-clean (no CTA language)
- comunidade-us.json tagline contains the brand lema "Se sozinho você já brilha, juntos iluminamos"
- curso-auriculo.json description is ≥120 chars and SEO-clean (no "botão principal" in description)
- Zero emojis across all 7 product JSONs
- Zero CTA URL or externalSiteUrl changes
- Brand voice present where copy was added: professional, acolhedor, inspirador, firme; "Nós" voice
</success_criteria>

<output>
After completion, create `/home/mauricio/gpus/.planning/phases/02-content-and-copy-overhaul/02-02-SUMMARY.md`
</output>
