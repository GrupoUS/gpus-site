---
phase: 05-seo-technical-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/assets/og-svg/home.svg
  - .planning/assets/og-svg/sobre.svg
  - .planning/assets/og-svg/contato.svg
  - .planning/assets/og-svg/curso-auriculo.svg
  - .planning/assets/og-svg/mentoria-black-neon.svg
  - .planning/assets/og-svg/otb.svg
  - .planning/assets/og-svg/termos.svg
  - .planning/assets/og-svg/privacidade.svg
  - .planning/assets/og-svg/404.svg
  - src/pages/index.astro
  - src/pages/sobre.astro
  - src/pages/contato.astro
  - src/pages/termos.astro
  - src/pages/politica-de-privacidade.astro
  - src/pages/404.astro
autonomous: false
requirements:
  - SEO-03
user_setup: []
must_haves:
  truths:
    - "9 SVG source files exist in .planning/assets/og-svg/ with navy bg + gold title + Grupo US brand"
    - "Every content page passes ogImage prop pointing to /og/{slug}.png"
    - "Build output HTML contains og:image meta with /og/ path for each page"
  artifacts:
    - path: ".planning/assets/og-svg/home.svg"
      provides: "OG image SVG source for home page"
      contains: "Grupo US"
    - path: ".planning/assets/og-svg/otb.svg"
      provides: "OG image SVG source for OTB page"
      contains: "OTB"
    - path: ".planning/assets/og-svg/termos.svg"
      provides: "OG image SVG source for termos page (legal variant)"
      contains: "Termos de Uso"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/Layout.astro"
      via: "ogImage prop"
      pattern: 'ogImage="/og/home.png"'
    - from: "src/pages/sobre.astro"
      to: "src/layouts/Layout.astro"
      via: "ogImage prop"
      pattern: 'ogImage="/og/sobre.png"'
---

<objective>
Create 9 SVG OG image source files (1200x630, navy bg, gold Playfair Display title, Grupo US brand) and wire the `ogImage` prop on all content pages to reference the per-page PNG files in `public/og/`.

Purpose: Close SEO-03 (per-page OG images for better social media preview). The SVG source files are created by Claude; the user exports to PNG and places in `public/og/`. The ogImage prop wiring ensures the build output references the correct per-page image.

Output: 9 SVG files in `.planning/assets/og-svg/`, all pages reference `/og/{slug}.png` via ogImage prop.
</objective>

<execution_context>
@/home/mauricio/gpus/.claude/get-shit-done/workflows/execute-plan.md
@/home/mauricio/gpus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-technical-layer/05-CONTEXT.md
@.planning/phases/05-seo-technical-layer/05-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/layouts/Layout.astro (ogImage prop):
```typescript
interface Props {
  title: string;
  description?: string;
  ogImage?: string;  // default: "/og-image.png"
  // ...
}
// In head:
// <meta property="og:image" content={new URL(ogImage, Astro.site)} />
```

From src/pages/index.astro (current, no ogImage prop, uses default):
```typescript
// <Layout title="Grupo US -- Formacao, mentoria e comunidade em saude estetica avancada" ...>
```

From src/pages/sobre.astro (current, no ogImage prop, uses default):
```typescript
// <Layout title="Sobre o Grupo US -- ecossistema de saude estetica avancada" ...>
```

Pages that already have ogImage: mentoria-black-neon.astro has `ogImage={d.image}`.
Pages from Plan 01 that will have ogImage: curso-auriculo.astro, otb.astro (set in Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 9 SVG OG image source files</name>
  <files>.planning/assets/og-svg/home.svg, .planning/assets/og-svg/sobre.svg, .planning/assets/og-svg/contato.svg, .planning/assets/og-svg/curso-auriculo.svg, .planning/assets/og-svg/mentoria-black-neon.svg, .planning/assets/og-svg/otb.svg, .planning/assets/og-svg/termos.svg, .planning/assets/og-svg/privacidade.svg, .planning/assets/og-svg/404.svg</files>
  <read_first>
    - .planning/phases/05-seo-technical-layer/05-UI-SPEC.md (OG image visual specification, layout grid, typography, color contract, per-page copy table)
    - .planning/phases/05-seo-technical-layer/05-RESEARCH.md (SVG OG image template example)
  </read_first>
  <action>
Create directory `.planning/assets/og-svg/` and generate 9 SVG files following the exact visual specification from the UI-SPEC.

**Shared SVG template (1200x630):**
- Background: `<rect width="1200" height="630" fill="#1a1a2e"/>` (navy)
- Page title: `<text x="60" y="280" font-family="Playfair Display, serif" font-size="64" font-weight="700" fill="#d4af37">` (gold, left-aligned)
- Subtitle: `<text x="60" y="340" font-family="Inter, sans-serif" font-size="24" fill="#94a3b8">` (text-muted)
- Gold accent line: `<rect x="60" y="400" width="200" height="3" fill="#d4af37" rx="1.5"/>`
- Brand wordmark: `<text x="1140" y="580" font-family="Playfair Display, serif" font-size="28" font-weight="600" fill="#d4af37" text-anchor="end">Grupo US</text>`

**Per-page title and subtitle values (from UI-SPEC copy contract):**

| File | Title | Subtitle |
|------|-------|----------|
| home.svg | "Grupo US" | "Formacao, mentoria e comunidade em saude estetica" |
| sobre.svg | "Sobre o Grupo US" | "Ecossistema de saude estetica avancada" |
| contato.svg | "Contato" | "Fale com o Grupo US" |
| curso-auriculo.svg | "Curso de Auriculo" | "Auriculoterapia e perfuracao presencial" |
| mentoria-black-neon.svg | "Mentoria Black NEON" | "Escala para clinicas de saude estetica" |
| otb.svg | "OTB -- Out Of The Box" | "MBA Internacional em Business Aesthetic Health" |
| termos.svg | "Termos de Uso" | "Grupo US" (legal variant: 48px title instead of 64px) |
| privacidade.svg | "Politica de Privacidade" | "Grupo US" (legal variant: 48px title instead of 64px) |
| 404.svg | "Pagina nao encontrada" | "Grupo US" (legal variant: 48px title instead of 64px) |

**Legal variant (termos, privacidade, 404):** Use 48px font-size for title instead of 64px. All other dimensions remain the same.

Note: SVG text uses unaccented characters intentionally -- font rendering at PNG export time will be what matters. The user may wish to add accents at export time in a design tool.
  </action>
  <verify>
    <automated>cd /home/mauricio/gpus && ls -la .planning/assets/og-svg/*.svg | wc -l && grep -c 'fill="#1a1a2e"' .planning/assets/og-svg/home.svg && grep -c 'Grupo US' .planning/assets/og-svg/home.svg</automated>
  </verify>
  <acceptance_criteria>
    - 9 SVG files exist in .planning/assets/og-svg/: home.svg, sobre.svg, contato.svg, curso-auriculo.svg, mentoria-black-neon.svg, otb.svg, termos.svg, privacidade.svg, 404.svg
    - Each SVG has `width="1200" height="630"` in the root element
    - Each SVG contains `fill="#1a1a2e"` (navy background rect)
    - Each SVG contains `fill="#d4af37"` (gold title text)
    - Each SVG contains `fill="#94a3b8"` (muted subtitle)
    - Each SVG contains `Grupo US` (brand wordmark)
    - home.svg title text contains "Grupo US"
    - otb.svg title text contains "OTB"
    - termos.svg has `font-size="48"` for title (legal variant)
    - privacidade.svg has `font-size="48"` for title (legal variant)
  </acceptance_criteria>
  <done>9 SVG OG image source files exist with correct dimensions, navy/gold color scheme, per-page titles and subtitles matching the UI-SPEC copy contract.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ogImage prop on all content pages</name>
  <files>src/pages/index.astro, src/pages/sobre.astro, src/pages/contato.astro, src/pages/termos.astro, src/pages/politica-de-privacidade.astro, src/pages/404.astro</files>
  <read_first>
    - src/pages/index.astro (current Layout call, needs ogImage="/og/home.png")
    - src/pages/sobre.astro (current Layout call, needs ogImage="/og/sobre.png")
    - src/pages/contato.astro (current Layout call, needs ogImage="/og/contato.png")
    - src/pages/termos.astro (current Layout call, needs ogImage="/og/termos.png")
    - src/pages/politica-de-privacidade.astro (current Layout call, needs ogImage="/og/privacidade.png")
    - src/pages/404.astro (current Layout call, needs ogImage="/og/404.png")
    - src/pages/curso-auriculo.astro (check if ogImage already set, add if not)
    - src/pages/mentoria-black-neon.astro (currently has ogImage={d.image}, update to /og/mentoria-black-neon.png)
  </read_first>
  <action>
Add `ogImage` prop to every page's Layout tag. Pages being modified by Plan 01 (curso-auriculo, mentoria-black-neon, otb) will also need this if not already set there.

**Pages to update in THIS task:**

1. **src/pages/index.astro:** Add `ogImage="/og/home.png"` to Layout props
2. **src/pages/sobre.astro:** Add `ogImage="/og/sobre.png"` to Layout props
3. **src/pages/contato.astro:** Add `ogImage="/og/contato.png"` to Layout props
4. **src/pages/termos.astro:** Add `ogImage="/og/termos.png"` to Layout props
5. **src/pages/politica-de-privacidade.astro:** Add `ogImage="/og/privacidade.png"` to Layout props
6. **src/pages/404.astro:** Add `ogImage="/og/404.png"` to Layout props

**Pages that Plan 01 handles (verify/update if not done):**
- **curso-auriculo.astro:** Should have `ogImage="/og/curso-auriculo.png"` (add if Plan 01 did not set it)
- **mentoria-black-neon.astro:** Currently has `ogImage={d.image}`. Replace with `ogImage="/og/mentoria-black-neon.png"` for consistency (d.image is the SVG product icon, not an OG image)
- **otb.astro:** Should have `ogImage="/og/otb.png"` (set in Plan 01)

The prop is added to the Layout opening tag alongside existing props. Example for index.astro:
```html
<Layout
  title="Grupo US -- ..."
  description="..."
  ogImage="/og/home.png"
  activeNav="home"
>
```

After all changes: `bun run build` and then verify `grep 'og:image' dist/*/index.html` shows per-page paths.

NOTE: The actual PNG files in `public/og/` do NOT need to exist for the build to succeed. The ogImage prop just sets the meta tag URL. The user will export PNGs from SVGs and place them in `public/og/` before deploy. Create the `public/og/` directory with a `.gitkeep` file so the directory exists in the repo.
  </action>
  <verify>
    <automated>cd /home/mauricio/gpus && bun run lint && bunx astro check && bun run build && grep -o 'og/[a-z0-9-]*\.png' dist/index.html && grep -o 'og/[a-z0-9-]*\.png' dist/sobre/index.html && grep -o 'og/[a-z0-9-]*\.png' dist/contato/index.html</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/index.astro contains `ogImage="/og/home.png"`
    - src/pages/sobre.astro contains `ogImage="/og/sobre.png"`
    - src/pages/contato.astro contains `ogImage="/og/contato.png"`
    - src/pages/termos.astro contains `ogImage="/og/termos.png"`
    - src/pages/politica-de-privacidade.astro contains `ogImage="/og/privacidade.png"`
    - src/pages/404.astro contains `ogImage="/og/404.png"`
    - src/pages/mentoria-black-neon.astro contains `ogImage="/og/mentoria-black-neon.png"` (not d.image)
    - src/pages/curso-auriculo.astro contains `ogImage="/og/curso-auriculo.png"`
    - public/og/.gitkeep exists
    - `bun run build` exits 0
    - `grep 'og/home.png' dist/index.html` has output
    - `grep 'og/sobre.png' dist/sobre/index.html` has output
  </acceptance_criteria>
  <done>All 9 content pages (including 404) pass ogImage prop pointing to per-page PNG in /og/ directory. public/og/.gitkeep tracks the directory. Build succeeds and meta tags reference correct paths.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify OG image SVG visual quality and export to PNG</name>
  <what-built>9 SVG OG image source files in .planning/assets/og-svg/ with navy background, gold Playfair Display title, subtitle, accent line, and Grupo US brand wordmark. All pages wired to reference /og/{slug}.png via ogImage prop.</what-built>
  <how-to-verify>
    1. Open a few SVG files in your browser to verify the visual layout:
       - `open .planning/assets/og-svg/home.svg`
       - `open .planning/assets/og-svg/otb.svg`
       - `open .planning/assets/og-svg/termos.svg` (legal variant with smaller title)
    2. Verify navy background (#1a1a2e), gold title text (#d4af37), muted subtitle (#94a3b8), gold accent line, and "Grupo US" wordmark bottom-right
    3. Export SVGs to PNG (1200x630) using your preferred tool (Figma, Inkscape, browser screenshot) and place in `public/og/` with matching filenames
    4. After placing PNGs: run `bun run build` and verify `dist/` output has correct og:image meta tags
  </how-to-verify>
  <resume-signal>Type "approved" after exporting PNGs to public/og/, or describe any visual adjustments needed to the SVG templates</resume-signal>
</task>

</tasks>

<verification>
1. `ls .planning/assets/og-svg/*.svg | wc -l` -- expect 9
2. `bun run build` succeeds
3. `grep -r 'og:image.*og/' dist/ | head -9` -- each page references /og/{slug}.png
4. Visual check: SVGs render correctly in browser with navy/gold brand colors
5. After user exports PNGs: `ls public/og/*.png | wc -l` -- expect 9
</verification>

<success_criteria>
- 9 SVG OG image source files in .planning/assets/og-svg/ matching the UI-SPEC visual contract
- All 9 content pages (8 originals + OTB) pass ogImage prop to Layout
- Build output og:image meta tags reference per-page /og/{slug}.png paths
- User has approved SVG visual quality and exported PNGs to public/og/
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-technical-layer/05-02-SUMMARY.md`
</output>
