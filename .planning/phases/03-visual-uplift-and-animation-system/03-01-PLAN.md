---
phase: 03-visual-uplift-and-animation-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/aurora-background.tsx
  - src/styles/global.css
  - src/components/shared/Button.astro
  - src/components/landing/LandingHero.astro
  - src/components/landing/LandingCTA.astro
  - src/components/home/CTASection.astro
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-06]

must_haves:
  truths:
    - "Home hero aurora background uses gold/navy brand colors instead of blue/indigo"
    - "Landing hero sections have animated mesh gradient background with ~20s cycle"
    - "CTA sections (home + landing) use glass-card-bright with stronger gold glow"
    - "All 4 button variants (primary, outline, ghost, whatsapp) have hover glow shadows"
    - "Mesh gradient animation disabled on mobile (max-width 768px) and prefers-reduced-motion"
  artifacts:
    - path: "src/components/ui/aurora-background.tsx"
      provides: "Navy/gold aurora gradient replacing blue/indigo"
      contains: "color-gold"
    - path: "src/styles/global.css"
      provides: "glass-card-bright utility, landing-mesh-bg utility, mesh-drift keyframe, glow-card-hover styles"
      contains: "glass-card-bright"
    - path: "src/components/shared/Button.astro"
      provides: "Hover glow box-shadow on outline, ghost, whatsapp variants"
      contains: "hover:shadow"
    - path: "src/components/landing/LandingHero.astro"
      provides: "Mesh gradient background replacing static radial blur"
      contains: "landing-mesh-bg"
    - path: "src/components/landing/LandingCTA.astro"
      provides: "glass-card-bright class on CTA container"
      contains: "glass-card-bright"
    - path: "src/components/home/CTASection.astro"
      provides: "glass-card-bright class on CTA card area"
      contains: "glass-card-bright"
  key_links:
    - from: "src/styles/global.css"
      to: "src/components/landing/LandingHero.astro"
      via: "landing-mesh-bg utility class"
      pattern: "landing-mesh-bg"
    - from: "src/styles/global.css"
      to: "src/components/landing/LandingCTA.astro"
      via: "glass-card-bright utility class"
      pattern: "glass-card-bright"
    - from: "src/styles/global.css"
      to: "src/components/home/CTASection.astro"
      via: "glass-card-bright utility class"
      pattern: "glass-card-bright"
---

<objective>
Establish the CSS foundation for the premium visual identity: navy/gold aurora hero, animated mesh gradient for landing pages, glass-card-bright CTA variant, and consistent button hover glows across all 4 variants.

Purpose: This plan creates all CSS utilities, keyframes, and component-level class changes that the rest of Phase 3 builds upon. Aurora color fix (VIS-01), mesh gradient (VIS-02), glass-card evolution (VIS-03), and button hovers (VIS-06) are all CSS-centric work.

Output: Updated global.css with new utilities/keyframes, brand-colored aurora, glass-card-bright on CTA sections, mesh gradient on landing heroes, and hover glow on all button variants.
</objective>

<execution_context>
@/home/mauricio/gpus/.claude/get-shit-done/workflows/execute-plan.md
@/home/mauricio/gpus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visual-uplift-and-animation-system/03-CONTEXT.md
@.planning/phases/03-visual-uplift-and-animation-system/03-RESEARCH.md
@.planning/phases/03-visual-uplift-and-animation-system/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/ui/aurora-background.tsx:
```tsx
interface AuroraBackgroundProps extends ComponentPropsWithoutRef<"div"> {
  children: ReactNode;
  showRadialGradient?: boolean;
}
export const AuroraBackground = ({ className, children, showRadialGradient = true, ...props }: AuroraBackgroundProps) => { ... }
```

From src/components/shared/Button.astro:
```ts
interface Props {
  variant?: "primary" | "whatsapp" | "outline" | "ghost";
  href?: string;
  size?: "sm" | "md" | "lg";
  class?: string;
  target?: string;
  ariaLabel?: string;
}
const variantClasses: Record<string, string> = {
  primary: "bg-gold text-navy hover:bg-gold-light gold-glow hover:scale-[1.02] active:scale-95",
  whatsapp: "bg-whatsapp text-white hover:bg-whatsapp-hover hover:shadow-lg hover:shadow-whatsapp/20 active:scale-95",
  outline: "border-2 border-gold text-gold hover:bg-gold hover:text-navy hover:shadow-lg hover:shadow-gold/20 active:scale-95",
  ghost: "text-gold hover:bg-navy-lighter/50 active:scale-95",
};
```

From src/components/landing/LandingHero.astro Props:
```ts
interface Props {
  name: string;
  tagline: string;
  hero: { headline: string; subheadline: string; };
  type: string;
  cta: { label: string; url: string; whatsappMessage: string; helperText?: string; };
}
```

Current global.css utility pattern:
```css
@utility glass-card {
  background: linear-gradient(...), radial-gradient(...);
  border: 1px solid color-mix(in srgb, var(--color-gold) 20%, transparent);
  backdrop-filter: blur(18px) saturate(125%);
  box-shadow: 0 24px 70px ..., inset 0 1px 0 ...;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aurora navy/gold color customization (VIS-01)</name>
  <files>src/components/ui/aurora-background.tsx</files>
  <read_first>
    - src/components/ui/aurora-background.tsx
    - src/styles/global.css (for color token names)
    - src/components/home/Hero.astro (consumer of AuroraBackground)
  </read_first>
  <action>
    Modify aurora-background.tsx to replace Aceternity default blue/indigo palette with navy/gold brand colors per D-01 and D-02.

    **Specific changes to the inline style object (line ~28-43):**
    Replace the CSS custom properties block:
    - `"--aurora"` value: change from `"repeating-linear-gradient(100deg,#3b82f6_10%,#a5b4fc_15%,#93c5fd_20%,#ddd6fe_25%,#60a5fa_30%)"` to `"repeating-linear-gradient(100deg,var(--color-gold)_10%,var(--color-gold-light)_15%,var(--color-navy-lighter)_20%,var(--color-gold-dark)_25%,var(--color-gold)_30%)"`
    - Remove `"--blue-300"`, `"--blue-400"`, `"--blue-500"`, `"--indigo-300"`, `"--violet-200"` CSS variable declarations entirely
    - Keep `"--black"`, `"--white"`, `"--transparent"` as they are used by the gradient layering logic
    - Replace `"--dark-gradient"` and `"--white-gradient"` values: keep the repeating-linear-gradient structure but these use `--black`, `--white`, `--transparent` which remain unchanged

    **Specific changes to the wrapper div (line ~18-21):**
    Replace `className` from:
    `"transition-bg relative flex h-[100vh] flex-col items-center justify-center bg-zinc-50 text-slate-950 dark:bg-zinc-900"`
    to:
    `"transition-bg relative flex h-[100vh] flex-col items-center justify-center bg-navy"`

    **Specific changes to the inner gradient div (line ~48-53):**
    In the long className string:
    - Replace `[--aurora:repeating-linear-gradient(100deg,var(--blue-500)_10%,var(--indigo-300)_15%,var(--blue-300)_20%,var(--violet-200)_25%,var(--blue-400)_30%)]` with `[--aurora:repeating-linear-gradient(100deg,var(--color-gold)_10%,var(--color-gold-light)_15%,var(--color-navy-lighter)_20%,var(--color-gold-dark)_25%,var(--color-gold)_30%)]`
    - Remove `dark:invert-0` (no dark mode toggle exists)
    - Remove `dark:[background-image:var(--dark-gradient),var(--aurora)]`
    - Remove `after:dark:[background-image:var(--dark-gradient),var(--aurora)]`
    - Change `[background-image:var(--white-gradient),var(--aurora)]` to `[background-image:var(--dark-gradient),var(--aurora)]` (site is always dark)
    - Change `after:[background-image:var(--white-gradient),var(--aurora)]` to `after:[background-image:var(--dark-gradient),var(--aurora)]`
    - Remove `invert filter` classes (the invert trick is for light-mode; dark-navy site does not need it)

    **Do NOT change:** The `showRadialGradient` mask-image prop, the `animate-aurora` utility reference, the `will-change-transform`, the `blur-[10px]`, the `after:` pseudo-element structure, or the component's Props interface/export.
  </action>
  <verify>
    <automated>cd /home/mauricio/gpus && grep -c 'color-gold' src/components/ui/aurora-background.tsx | grep -qv '^0$' && grep -c 'blue-500\|indigo-300\|violet-200' src/components/ui/aurora-background.tsx | grep -q '^0$' && grep -c 'bg-zinc-50\|text-slate-950\|dark:bg-zinc-900' src/components/ui/aurora-background.tsx | grep -q '^0$' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - aurora-background.tsx contains `color-gold` at least 4 times (in --aurora gradient stops and inline overrides)
    - aurora-background.tsx contains zero occurrences of `blue-500`, `indigo-300`, `violet-200`
    - aurora-background.tsx contains zero occurrences of `bg-zinc-50`, `text-slate-950`, `dark:bg-zinc-900`
    - aurora-background.tsx contains `bg-navy` in the wrapper div className
    - aurora-background.tsx does not contain `dark:invert-0` or `after:dark:`
    - `bun run build` succeeds (no broken import or class reference)
  </acceptance_criteria>
  <done>Aurora background renders navy/gold gradient instead of blue/indigo. No dark: class prefixes remain. Wrapper uses bg-navy.</done>
</task>

<task type="auto">
  <name>Task 2: CSS utilities and keyframes in global.css (VIS-02, VIS-03, VIS-06)</name>
  <files>src/styles/global.css</files>
  <read_first>
    - src/styles/global.css
    - .planning/phases/03-visual-uplift-and-animation-system/03-UI-SPEC.md (animation contract section)
    - .planning/phases/03-visual-uplift-and-animation-system/03-RESEARCH.md (Pattern 3: CSS Mesh Gradient, glass-card-bright example, button glow examples)
  </read_first>
  <action>
    Add the following to src/styles/global.css. Place new additions AFTER the existing `card-glow-hover` section and BEFORE the `text-shimmer` keyframe section.

    **1. Glass-card-bright utility (VIS-03, D-06):**
    ```css
    @utility glass-card-bright {
      background:
        linear-gradient(
          180deg,
          color-mix(in srgb, var(--color-navy-light) 85%, white 8%) 0%,
          color-mix(in srgb, var(--color-navy) 88%, transparent) 100%
        ),
        radial-gradient(
          circle at top left,
          color-mix(in srgb, var(--color-gold) 18%, transparent) 0%,
          transparent 50%
        );
      border: 1px solid color-mix(in srgb, var(--color-gold) 35%, transparent);
      backdrop-filter: blur(24px) saturate(140%);
      box-shadow:
        0 0 40px color-mix(in srgb, var(--color-gold) 15%, transparent),
        0 24px 70px color-mix(in srgb, var(--color-navy) 45%, transparent),
        inset 0 1px 0 color-mix(in srgb, white 10%, transparent);
    }
    ```

    **2. Glass-card refinement (VIS-03, D-05):**
    Update the existing `@utility glass-card` — change the gold radial gradient from `12%` to `14%` opacity and add a subtle outer glow:
    - Change: `color-mix(in srgb, var(--color-gold) 12%, transparent)` to `color-mix(in srgb, var(--color-gold) 14%, transparent)`
    - Add to the box-shadow list: `0 0 20px color-mix(in srgb, var(--color-gold) 6%, transparent)` as the first shadow layer (before the existing `0 24px 70px` shadow)

    **3. Mesh gradient keyframe and utility (VIS-02, D-03, D-04):**
    ```css
    @keyframes mesh-drift {
      0%, 100% {
        background-position: 0% 50%, 100% 50%, 50% 0%;
      }
      33% {
        background-position: 100% 50%, 0% 100%, 50% 100%;
      }
      66% {
        background-position: 50% 100%, 50% 0%, 0% 50%;
      }
    }

    @utility landing-mesh-bg {
      background:
        radial-gradient(ellipse at 25% 25%, color-mix(in srgb, var(--color-gold) 8%, transparent) 0%, transparent 50%),
        radial-gradient(ellipse at 75% 75%, color-mix(in srgb, var(--color-gold-dark) 6%, transparent) 0%, transparent 50%),
        radial-gradient(ellipse at 50% 50%, color-mix(in srgb, var(--color-navy-lighter) 15%, transparent) 0%, transparent 60%);
      background-size: 200% 200%, 200% 200%, 200% 200%;
      animation: mesh-drift 20s ease-in-out infinite;
    }
    ```

    **4. Mobile disable for mesh-drift (D-04, matches existing pattern at line ~187):**
    Add inside the existing `@media (max-width: 768px)` block (the one that disables gold-pulse-glow and float-gentle):
    ```css
    .landing-mesh-bg {
      animation: none;
    }
    ```

    **5. Reduced motion for mesh-drift (D-04):**
    The existing `@media (prefers-reduced-motion: reduce)` block at lines 98-111 with `animation-duration: 0.01ms !important` already covers all animations globally. No additional rule needed — the `0.01ms` duration effectively kills mesh-drift. This is confirmed.

    **6. Mousemove glow card styles (VIS-04, D-11) — CSS portion:**
    ```css
    /* ===== Mousemove Glow Card ===== */

    [data-glow-card] {
      --mouse-x: 50%;
      --mouse-y: 50%;
      position: relative;
    }

    [data-glow-card]::before {
      content: "";
      position: absolute;
      inset: 0;
      border-radius: inherit;
      background: radial-gradient(
        300px circle at var(--mouse-x) var(--mouse-y),
        color-mix(in srgb, var(--color-gold) 12%, transparent),
        transparent 60%
      );
      opacity: 0;
      transition: opacity 0.3s ease;
      pointer-events: none;
      z-index: 1;
    }

    [data-glow-card]:hover::before {
      opacity: 1;
    }
    ```

    **Do NOT change:** Existing glass-card border value (stays 20% per D-07 — no shimmer), card-hover-lift, gold-pulse-glow, data-reveal system, or any existing keyframe. Do NOT add animated border shimmer (deferred). Do NOT add glow to landing page cards (deferred — ProductsGrid only per D-12).
  </action>
  <verify>
    <automated>cd /home/mauricio/gpus && grep -q 'glass-card-bright' src/styles/global.css && grep -q 'mesh-drift' src/styles/global.css && grep -q 'landing-mesh-bg' src/styles/global.css && grep -q 'data-glow-card' src/styles/global.css && bun run lint && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - global.css contains `@utility glass-card-bright` with `backdrop-filter: blur(24px) saturate(140%)`
    - global.css contains `@keyframes mesh-drift` with 3 stops (0%/100%, 33%, 66%)
    - global.css contains `@utility landing-mesh-bg` with 3 radial-gradient layers and `animation: mesh-drift 20s`
    - global.css contains `[data-glow-card]::before` with `radial-gradient(300px circle at var(--mouse-x) var(--mouse-y)`
    - global.css `@media (max-width: 768px)` block contains `.landing-mesh-bg { animation: none; }`
    - Existing glass-card utility gold radial now uses `14%` instead of `12%`
    - Existing glass-card box-shadow has new `0 0 20px` gold glow layer
    - `bun run lint` passes
  </acceptance_criteria>
  <done>All new CSS utilities (glass-card-bright, landing-mesh-bg, mousemove glow styles) and keyframes (mesh-drift) exist in global.css. Glass-card refined with slightly stronger gold tint. Mobile and reduced-motion fallbacks in place.</done>
</task>

<task type="auto">
  <name>Task 3: Apply CSS classes to Button, LandingHero, LandingCTA, CTASection (VIS-02, VIS-03, VIS-06)</name>
  <files>src/components/shared/Button.astro, src/components/landing/LandingHero.astro, src/components/landing/LandingCTA.astro, src/components/home/CTASection.astro</files>
  <read_first>
    - src/components/shared/Button.astro
    - src/components/landing/LandingHero.astro
    - src/components/landing/LandingCTA.astro
    - src/components/home/CTASection.astro
    - src/styles/global.css (to confirm new utilities exist from Task 2)
  </read_first>
  <action>
    **A. Button.astro — add hover glow shadows (VIS-06, D-13):**

    Update the `variantClasses` record. The primary variant already has `gold-glow` — no change needed. Replace the existing shadow utilities on outline/whatsapp with a single combined arbitrary shadow that includes both elevation and glow. In Tailwind v4, `hover:shadow-[...]` with comma-separated shadows works:

    ```ts
    const variantClasses: Record<string, string> = {
      primary:
        "bg-gold text-navy hover:bg-gold-light gold-glow hover:scale-[1.02] active:scale-95",
      whatsapp:
        "bg-whatsapp text-white hover:bg-whatsapp-hover hover:shadow-[0_4px_12px_var(--color-whatsapp)/10%,0_0_20px_var(--color-whatsapp)/25%] active:scale-95",
      outline:
        "border-2 border-gold text-gold hover:bg-gold hover:text-navy hover:shadow-[0_4px_12px_var(--color-gold)/10%,0_0_20px_var(--color-gold)/25%] active:scale-95",
      ghost:
        "text-gold hover:bg-navy-lighter/50 hover:shadow-[0_0_15px_var(--color-gold)/15%] active:scale-95",
    };
    ```

    This replaces the old `hover:shadow-lg hover:shadow-gold/20` (which used two conflicting shadow utilities) with a single `hover:shadow-[...]` containing both elevation and glow layers. Verify with `bun run build` — if Tailwind v4 rejects the arbitrary syntax, add utility classes `.btn-glow-outline`, `.btn-glow-ghost`, `.btn-glow-whatsapp` in global.css instead.

    **B. LandingHero.astro — apply mesh gradient background (VIS-02, D-03):**

    Replace the current static radial gradient divs (lines 34-39):
    ```html
    <div class="absolute top-1/4 left-1/2 h-[600px] w-[800px] -translate-x-1/2 -translate-y-1/2 rounded-full bg-gold/5 blur-[120px]"></div>
    <div class="absolute right-0 bottom-0 h-[400px] w-[400px] rounded-full bg-gold/3 blur-[100px]"></div>
    ```

    Replace the entire `<div class="pointer-events-none absolute inset-0" aria-hidden="true">` container with:
    ```html
    <div class="landing-mesh-bg pointer-events-none absolute inset-0" aria-hidden="true"></div>
    ```

    This replaces two static blur blobs with the animated mesh gradient utility. The `landing-mesh-bg` utility defined in global.css provides the animated radial gradient background.

    **C. LandingCTA.astro — apply glass-card-bright (VIS-03, D-06):**

    On the inner content div (line 25), which currently has class `"relative z-10 mx-auto max-w-3xl text-center"` and `data-reveal="scale"`, wrap the content in a glass-card-bright container. Add `glass-card-bright rounded-2xl p-8 md:p-12` to the existing div:

    Change line 25 from:
    `<div class="relative z-10 mx-auto max-w-3xl text-center" data-reveal="scale">`
    to:
    `<div class="glass-card-bright relative z-10 mx-auto max-w-3xl rounded-2xl p-8 text-center md:p-12" data-reveal="scale">`

    **D. CTASection.astro — apply glass-card-bright (VIS-03, D-06):**

    The CTASection uses a LampBackdrop and has content in a nested div. Add `glass-card-bright rounded-2xl` to the inner content container. The inner div at line 20 (`class="relative z-50 mx-auto max-w-3xl text-center"`) should get `glass-card-bright rounded-2xl p-8 md:p-12`:

    Change:
    `<div class="relative z-50 mx-auto max-w-3xl text-center">`
    to:
    `<div class="glass-card-bright relative z-50 mx-auto max-w-3xl rounded-2xl p-8 text-center md:p-12">`
  </action>
  <verify>
    <automated>cd /home/mauricio/gpus && grep -q 'landing-mesh-bg' src/components/landing/LandingHero.astro && grep -q 'glass-card-bright' src/components/landing/LandingCTA.astro && grep -q 'glass-card-bright' src/components/home/CTASection.astro && grep -q 'hover:shadow' src/components/shared/Button.astro && bunx astro check && bun run build && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - LandingHero.astro contains `landing-mesh-bg` class on the background div
    - LandingHero.astro no longer contains `blur-[120px]` or `blur-[100px]` (static blobs removed)
    - LandingCTA.astro contains `glass-card-bright` on the inner content div
    - CTASection.astro contains `glass-card-bright` on the inner content div
    - Button.astro outline variant contains hover shadow with gold glow
    - Button.astro ghost variant contains hover shadow with gold glow
    - Button.astro whatsapp variant contains hover shadow with whatsapp glow
    - `bunx astro check` passes (no type errors)
    - `bun run build` succeeds
  </acceptance_criteria>
  <done>Mesh gradient active on landing heroes, glass-card-bright applied to both CTA sections, all button variants have consistent hover glow shadows. Build passes.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `bun run lint` passes
2. `bunx astro check` passes
3. `bun run build` succeeds
4. `grep -c 'color-gold' src/components/ui/aurora-background.tsx` returns >= 4
5. `grep 'glass-card-bright' src/styles/global.css` shows the utility definition
6. `grep 'landing-mesh-bg' src/components/landing/LandingHero.astro` confirms mesh gradient applied
7. `grep 'glass-card-bright' src/components/landing/LandingCTA.astro` confirms CTA upgrade
8. No `blue-500`, `indigo-300`, `violet-200` in aurora-background.tsx
</verification>

<success_criteria>
- VIS-01: Aurora hero uses navy/gold brand colors (gold, gold-light, navy-lighter, gold-dark tokens)
- VIS-02: Landing heroes display animated mesh gradient with 20s cycle, disabled on mobile and reduced-motion
- VIS-03: glass-card refined (14% gold tint, subtle outer glow) + glass-card-bright exists and applied to CTA sections
- VIS-06: All 4 button variants have hover glow box-shadows (primary already had gold-glow, now outline/ghost/whatsapp match)
- All validation gates pass: lint, astro check, build
</success_criteria>

<output>
After completion, create `.planning/phases/03-visual-uplift-and-animation-system/03-01-SUMMARY.md`
</output>
