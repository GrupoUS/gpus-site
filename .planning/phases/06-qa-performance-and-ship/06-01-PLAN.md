---
phase: 06-qa-performance-and-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/lamp.tsx
  - src/components/ui/aurora-background.tsx
  - src/components/ui/text-generate-effect.tsx
  - src/components/ui/background-beams.tsx
  - src/components/ui/HeroEntrance.tsx
  - src/components/ui/LandingHeroEntrance.tsx
  - src/components/ui/MotionReveal.tsx
  - src/components/ui/AnimatedStats.tsx
  - src/components/home/JourneyTimeline.tsx
  - src/components/landing/TestimonialCarousel.tsx
  - src/components/shared/WhatsAppFloatingButton.tsx
  - src/components/landing/NeonBio.astro
  - src/components/home/Hero.astro
  - src/components/ui/wavy-background.tsx
  - src/components/ui/moving-border.tsx
  - package.json
  - AGENTS.md
autonomous: true
requirements:
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-15
  - D-17

must_haves:
  truths:
    - "lamp.tsx animates only transform/opacity — no width/height/top/left"
    - "No 'use client' directives remain in any src/components/ file"
    - "No hardcoded hex values in NeonBio.astro or Hero.astro Spotlight prop"
    - "framer-motion and simplex-noise removed from package.json"
    - "wavy-background.tsx and moving-border.tsx deleted"
    - "aurora-background.tsx has no background-attachment:fixed"
    - "lamp.tsx, background-beams.tsx, text-generate-effect.tsx all have useReducedMotion"
    - "AGENTS.md reflects team=13, motion 12.x, no contact/ note"
  artifacts:
    - path: "src/components/ui/lamp.tsx"
      provides: "scaleX-based lamp animation with useReducedMotion"
      contains: "scaleX"
    - path: "package.json"
      provides: "Clean dependency list"
      not_contains: "framer-motion"
    - path: "AGENTS.md"
      provides: "Accurate documentation"
      contains: "13 team member JSON files"
  key_links:
    - from: "src/components/ui/lamp.tsx"
      to: "motion/react"
      via: "useReducedMotion import"
      pattern: "useReducedMotion"
    - from: "src/components/ui/background-beams.tsx"
      to: "motion/react"
      via: "useReducedMotion import"
      pattern: "useReducedMotion"
---

<objective>
Technical debt cleanup and cross-browser/accessibility audit fixes across all React islands.

Purpose: Fix all known AGENTS.md violations (width animation, hardcoded hex, unused deps, stale docs) and add missing useReducedMotion guards to 3 Aceternity UI islands — so the Lighthouse audit in Plan 02 measures a clean, compliant codebase.

Output: Clean codebase with zero AGENTS.md violations, all islands accessibility-compliant, unused dependencies and dead components removed.
</objective>

<execution_context>
@/home/mauricio/gpus/.claude/get-shit-done/workflows/execute-plan.md
@/home/mauricio/gpus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-qa-performance-and-ship/06-CONTEXT.md
@.planning/phases/06-qa-performance-and-ship/06-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs for lamp.tsx scaleX refactor -->

Current lamp.tsx animation pattern (MUST REPLACE):
```tsx
initial={{ opacity: 0.5, width: "15rem" }}
whileInView={{ opacity: 1, width: "30rem" }}
```

Target pattern (per RESEARCH.md Pattern 3):
```tsx
initial={{ opacity: 0.5, scaleX: 0.5 }}
whileInView={{ opacity: 1, scaleX: 1 }}
// Container keeps w-[30rem], transformOrigin varies by element
```

Three animated divs in lamp.tsx need the refactor:
1. Lines 14-29: right-1/2 conic gradient — transformOrigin: 'right center'
2. Lines 30-45: left-1/2 conic gradient — transformOrigin: 'left center'
3. Lines 49-58: gold-light glow — initial scaleX: 0.5, w-64 (16rem base)
4. Lines 59-68: gold-light thin line — initial scaleX: 0.5, w-[30rem] base

useReducedMotion pattern (from existing islands like HeroEntrance.tsx):
```tsx
import { useReducedMotion } from "motion/react";
const shouldReduce = useReducedMotion();
if (shouldReduce) return <StaticFallback />;
```

NeonBio.astro line 14 current: `bg-[#fafaf9]` — replace with `bg-text-primary`
Hero.astro line 11 current: `fill="#d4af37"` — replace with `fill="var(--color-gold)"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix lamp.tsx width animation + add useReducedMotion, remove aurora bg-attachment:fixed, fix hex values</name>
  <files>
    src/components/ui/lamp.tsx,
    src/components/ui/aurora-background.tsx,
    src/components/landing/NeonBio.astro,
    src/components/home/Hero.astro
  </files>
  <read_first>
    src/components/ui/lamp.tsx,
    src/components/ui/aurora-background.tsx,
    src/components/landing/NeonBio.astro,
    src/components/home/Hero.astro,
    src/components/landing/NeonStory.astro,
    src/styles/global.css
  </read_first>
  <action>
**lamp.tsx (D-05 + D-17):**

1. Add `import { useReducedMotion } from "motion/react"` alongside the existing `motion` import. The full import line becomes: `import { motion, useReducedMotion } from "motion/react"`.

2. Inside `LampBackdrop`, add `const shouldReduce = useReducedMotion();` as the first line of the component body.

3. Add a reduced-motion early return BEFORE the main JSX: if `shouldReduce` is true, render the lamp at its final visual state (full opacity, full width, no animation). The static fallback should be the same JSX structure but with plain `<div>` instead of `<motion.div>`, and all elements at their final dimensions (w-[30rem], w-64, opacity-1). No motion imports needed in the fallback — just static divs with the same class names minus any motion props.

4. Replace ALL four `motion.div` width animations with scaleX:
   - **First conic gradient (right-1/2, lines ~14-29):** Change `initial={{ opacity: 0.5, width: "15rem" }}` to `initial={{ opacity: 0.5, scaleX: 0.5 }}` and `whileInView={{ opacity: 1, width: "30rem" }}` to `whileInView={{ opacity: 1, scaleX: 1 }}`. The className already has `w-[30rem]` — keep it. Add `style={{ backgroundImage: ..., transformOrigin: 'right center' }}` (merge with existing style prop).
   - **Second conic gradient (left-1/2, lines ~30-45):** Same scaleX change. Add `transformOrigin: 'left center'` to the style prop.
   - **Gold-light glow (lines ~49-58):** Change `initial={{ width: "8rem" }}` to `initial={{ scaleX: 0.5 }}` and `whileInView={{ width: "16rem" }}` to `whileInView={{ scaleX: 1 }}`. The className has `w-64` (=16rem) — keep it. Add `style={{ transformOrigin: 'center center' }}`.
   - **Gold-light thin line (lines ~59-68):** Change `initial={{ width: "15rem" }}` to `initial={{ scaleX: 0.5 }}` and `whileInView={{ width: "30rem" }}` to `whileInView={{ scaleX: 1 }}`. The className has `w-[30rem]` — keep it. Add `style={{ transformOrigin: 'center center' }}`.

5. Remove the `"use client"` directive from line 1 (per D-08, handled here since we are editing the file).

**aurora-background.tsx (D-15 + D-08):**

1. Remove `"use client"` from line 1.
2. In the long className string on the inner div (line ~39), find `after:[background-attachment:fixed]` and remove it. The aurora animation uses `background-position` keyframes which work without fixed attachment. The section fills the viewport so fixed attachment adds no visual benefit.

**NeonBio.astro (D-07):**

1. On line 14, replace `bg-[#fafaf9]` with `bg-text-primary`. This matches `--color-text-primary: #fafaf9` in global.css. NeonStory.astro already uses `bg-text-primary` for the same purpose.

**Hero.astro (D-07):**

1. On line 11, replace `fill="#d4af37"` with `fill="var(--color-gold)"`. The Spotlight component renders an inline SVG `<ellipse fill={fill}>` — CSS custom properties work in inline SVG attributes in all modern browsers.
  </action>
  <verify>
    <automated>cd /home/mauricio/gpus && grep -n 'width:.*rem' src/components/ui/lamp.tsx || echo "OK: no width rem animation" && grep -n 'scaleX' src/components/ui/lamp.tsx && grep -n 'useReducedMotion' src/components/ui/lamp.tsx && grep -n 'background-attachment' src/components/ui/aurora-background.tsx || echo "OK: no bg-attachment" && grep -n 'bg-\[#' src/components/landing/NeonBio.astro || echo "OK: no hex in NeonBio" && grep -n 'fill="#' src/components/home/Hero.astro || echo "OK: no hex fill in Hero" && bun run lint && bunx astro check && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'scaleX' src/components/ui/lamp.tsx` returns >= 4 (four scaleX animations)
    - `grep -c 'width:' src/components/ui/lamp.tsx` returns 0 (no width animations)
    - `grep 'useReducedMotion' src/components/ui/lamp.tsx` returns a match
    - `grep 'background-attachment' src/components/ui/aurora-background.tsx` returns no matches
    - `grep 'bg-\[#fafaf9\]' src/components/landing/NeonBio.astro` returns no matches
    - `grep 'bg-text-primary' src/components/landing/NeonBio.astro` returns a match
    - `grep 'fill="#d4af37"' src/components/home/Hero.astro` returns no matches
    - `grep 'var(--color-gold)' src/components/home/Hero.astro` returns a match
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>lamp.tsx uses scaleX with useReducedMotion fallback, aurora has no fixed bg-attachment, both hex values replaced with tokens</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused deps + dead files + "use client" directives + update AGENTS.md</name>
  <files>
    package.json,
    src/components/ui/wavy-background.tsx,
    src/components/ui/moving-border.tsx,
    src/components/ui/text-generate-effect.tsx,
    src/components/ui/background-beams.tsx,
    src/components/ui/HeroEntrance.tsx,
    src/components/ui/LandingHeroEntrance.tsx,
    src/components/ui/MotionReveal.tsx,
    src/components/ui/AnimatedStats.tsx,
    src/components/home/JourneyTimeline.tsx,
    src/components/landing/TestimonialCarousel.tsx,
    src/components/shared/WhatsAppFloatingButton.tsx,
    AGENTS.md
  </files>
  <read_first>
    package.json,
    AGENTS.md,
    src/components/ui/text-generate-effect.tsx,
    src/components/ui/background-beams.tsx,
    src/components/ui/HeroEntrance.tsx,
    src/components/ui/LandingHeroEntrance.tsx,
    src/components/ui/MotionReveal.tsx,
    src/components/ui/AnimatedStats.tsx,
    src/components/home/JourneyTimeline.tsx,
    src/components/landing/TestimonialCarousel.tsx,
    src/components/shared/WhatsAppFloatingButton.tsx
  </read_first>
  <action>
**Delete dead components (D-06):**

1. Delete `src/components/ui/wavy-background.tsx` (zero importers, only consumer of simplex-noise).
2. Delete `src/components/ui/moving-border.tsx` (zero importers).

**Remove unused deps from package.json (D-06):**

1. Remove `"framer-motion": "^12.38.0"` from dependencies. All imports use `motion/react` from the `motion` package.
2. Remove `"simplex-noise": "^4.0.3"` from dependencies. Only consumer (wavy-background.tsx) is being deleted.
3. Run `bun install` to update the lockfile.

**Remove "use client" from all remaining active files (D-08):**

Remove the `"use client";` line (always line 1) from these 11 files (lamp.tsx and aurora-background.tsx already handled in Task 1):
- `src/components/ui/text-generate-effect.tsx`
- `src/components/ui/background-beams.tsx`
- `src/components/ui/HeroEntrance.tsx`
- `src/components/ui/LandingHeroEntrance.tsx`
- `src/components/ui/MotionReveal.tsx`
- `src/components/ui/AnimatedStats.tsx`
- `src/components/home/JourneyTimeline.tsx`
- `src/components/landing/TestimonialCarousel.tsx`
- `src/components/shared/WhatsAppFloatingButton.tsx`

These are Next.js RSC directives that are no-ops in Astro. Astro uses `client:load`/`client:idle`/`client:visible` directives in `.astro` templates.

**Add useReducedMotion to text-generate-effect.tsx (D-17):**

1. Add `useReducedMotion` to the import from `"motion/react"`: `import { motion, stagger, useAnimate, useReducedMotion } from "motion/react"`.
2. Add `const shouldReduce = useReducedMotion();` inside the component body, after the existing hooks.
3. Modify the animation useEffect: wrap the `animate()` call in `if (shouldReduce) return;` at the top of the effect. When reduced motion is preferred, the words will appear via the existing `runMotion` state change (which shows them all at once via opacity-0 -> the animate call normally fades them in staggered; with reduced motion, set all spans to `opacity: 1` and `filter: "none"` immediately instead of staggered).
4. Simpler approach: in the stagger animation useEffect, if `shouldReduce`, call `animate("span", { opacity: 1, filter: "none" }, { duration: 0 })` instead of the staggered version. This instantly reveals all words.

**Add useReducedMotion to background-beams.tsx (D-17):**

1. Add `useReducedMotion` to the import: `import { motion, useReducedMotion } from "motion/react"`.
2. Inside the `React.memo` callback, add `const shouldReduce = useReducedMotion();`.
3. On each `<motion.linearGradient>` element, conditionally pass `animate` and `transition` props: if `shouldReduce`, pass `animate={undefined}` and `transition={undefined}` (or simply omit them). The simplest approach: use a ternary on the `animate` prop: `animate={shouldReduce ? undefined : { x1: ["0%", "100%"], ... }}` and same for `transition`.
4. This stops the continuous SVG gradient sweep animation for users with reduced motion preference, while keeping the static beam paths visible (decorative, `aria-hidden`).

**Update AGENTS.md stale info (D-09):**

1. Find line containing `team/           # 3 team member JSON files` (line ~117) and change to `team/           # 13 team member JSON files`.
2. Find line containing `Framer Motion` and `11.x` (line ~152) and change the version to `12.x`. Also update the library name column to say `motion (Framer Motion)` to reflect the actual package name.
3. Find line containing `contact/        # ContactForm (if extracted)` (line ~114) and remove the entire line (the directory does not exist).
4. Find line containing `3 JSON files (Sacha, Mauricio, Raquel)` (line ~313) and change to `13 JSON files` (remove the specific names since there are now 13 members).
  </action>
  <verify>
    <automated>cd /home/mauricio/gpus && test ! -f src/components/ui/wavy-background.tsx && echo "OK: wavy deleted" && test ! -f src/components/ui/moving-border.tsx && echo "OK: moving-border deleted" && grep -rn '"use client"' src/components/ || echo "OK: no use client" && grep 'framer-motion' package.json || echo "OK: no framer-motion" && grep 'simplex-noise' package.json || echo "OK: no simplex-noise" && grep '13 team member' AGENTS.md && grep '12\.x' AGENTS.md && grep 'useReducedMotion' src/components/ui/text-generate-effect.tsx && grep 'useReducedMotion' src/components/ui/background-beams.tsx && bun run lint && bunx astro check && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/ui/wavy-background.tsx` returns non-zero (file deleted)
    - `test -f src/components/ui/moving-border.tsx` returns non-zero (file deleted)
    - `grep -rc '"use client"' src/components/` returns 0 total matches across all files
    - `grep 'framer-motion' package.json` returns no matches
    - `grep 'simplex-noise' package.json` returns no matches
    - `grep '13 team member' AGENTS.md` returns a match
    - `grep '12\.x' AGENTS.md` returns a match
    - `grep 'ContactForm (if extracted)' AGENTS.md` returns no matches
    - `grep 'useReducedMotion' src/components/ui/text-generate-effect.tsx` returns a match
    - `grep 'useReducedMotion' src/components/ui/background-beams.tsx` returns a match
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>Unused deps/files removed, all "use client" directives gone, 3 Aceternity islands have useReducedMotion, AGENTS.md accurate</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **No AGENTS.md violations:**
   - `grep -rn 'width:.*rem' src/components/ui/lamp.tsx` returns nothing
   - `grep -rn 'bg-\[#' src/components/` returns nothing
   - `grep -rn '"use client"' src/components/` returns nothing
   - `grep -rn 'background-attachment' src/` returns nothing

2. **All islands accessibility-compliant:**
   - `grep -rn 'useReducedMotion' src/components/ui/lamp.tsx src/components/ui/text-generate-effect.tsx src/components/ui/background-beams.tsx` returns 3 matches

3. **Build passes clean:** `bun run lint && bunx astro check && bun run build` exits 0

4. **No dead dependencies:** `grep -E 'framer-motion|simplex-noise' package.json` returns nothing
</verification>

<success_criteria>
- Zero width/height/top/left animations across all React islands
- Zero "use client" directives in src/components/
- Zero hardcoded hex in NeonBio.astro and Hero.astro Spotlight prop
- framer-motion and simplex-noise removed from package.json
- wavy-background.tsx and moving-border.tsx deleted
- aurora-background.tsx has no background-attachment:fixed
- lamp.tsx, text-generate-effect.tsx, background-beams.tsx all have useReducedMotion guards
- AGENTS.md reflects 13 team members, motion 12.x, no contact/ directory
- `bun run lint && bunx astro check && bun run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-qa-performance-and-ship/06-01-SUMMARY.md`
</output>
