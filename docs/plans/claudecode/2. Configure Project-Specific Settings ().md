```markdown
Please configure my Claude Code setup implementing all the best practices, hidden settings, and optimizations to maximize efficiency, reduce friction, and improve context management.

Follow these exact steps to configure my environment:

### 1. Configure Global Settings (`~/.claude/settings.json`)
First, create or update my global settings file at `~/.claude/settings.json` with the following configurations:

```json
{
  "permissions": {
    "allow": [
      "Bash(npm run *)",
      "Bash(git commit *)",
      "Bash(git * main)",
      "Bash(* --version)",
      "Bash(* --help *)",
      "Read(~/.zshrc)"
    ],
    "deny": [
      "Bash(git push *)",
      "Read(./.env)",
      "Read(./.env.*)",
      "Read(./secrets/**)"
    ]
  },
  "cleanupPeriodDays": 90,
  "env": {
    "BASH_MAX_OUTPUT_LENGTH": "100000",
    "CLAUDE_CODE_FILE_READ_MAX_OUTPUT_TOKENS": "100000",
    "CLAUDE_AUTOCOMPACT_PCT_OVERRIDE": "75",
    "CLAUDE_CODE_ENABLE_TELEMETRY": "0"
  },
  "attribution": {
    "commit": "",
    "pr": ""
  },
  "includeCoAuthoredBy": false
}
```

### 2. Configure Project-Specific Settings (`.claude/settings.json`)
Next, create a project-specific settings file at `.claude/settings.json` to handle project-level configurations:

```json
{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(npm run test *)",
      "Bash(npm run lint)",
      "Bash(npm run build)"
    ]
  },
  "defaultMode": "auto"
}
```

### 3. Establish Path-Specific Rules (`.claude/rules/`)
Instead of overloading `CLAUDE.md`, create a directory structure for path-specific rules to ensure you follow project conventions without cluttering the main context:

1. Create directory `.claude/rules/`
2. Create a file `.claude/rules/frontend.md` for frontend-specific guidelines (if applicable)
3. Create a file `.claude/rules/backend.md` for backend-specific guidelines (if applicable)

### 4. Create Specialized Sub-Agents (`.claude/agents/`)
Create a specialized sub-agent for code review to isolate high-volume operations and preserve main context. Create `.claude/agents/code-reviewer.md`:

```yaml
---
name: code-reviewer
description: A code improvement agent that scans files and suggests improvements for readability, performance, and best practices.
model: claude-3-7-sonnet-20250219
tools: [Read, Grep, Glob, Bash]
---

You are a specialized code reviewer. Your task is to scan the provided files or directories, identify areas for improvement regarding readability, performance, and adherence to best practices. 

Always explain the issue, show the current code, and provide the improved version. Do not modify the files directly unless explicitly asked.
```

### 5. Acknowledge Best Practices for Large Files
Please acknowledge that you understand and will apply the following best practices when interacting with my codebase:

1. **Reading Large Files**: When dealing with large files, you will first check the line count using `wc -l` or similar tools, and then use `offset` and `limit` parameters to read the file in chunks to avoid silent omission of content.
2. **Context Management**: You will actively manage context by utilizing the auto-compaction trigger set to 75% to maintain output quality without losing critical information.
3. **Delegation**: You will use the `--agent` flag or delegate tasks to specialized sub-agents (like the code-reviewer) for specific, isolated tasks to prevent context pollution in the main conversation.
4. **Tool Granularity**: You will respect the fine-grained permission syntax (`Tool(specifier)`) and avoid using overly broad wildcards that could compromise security.

Please confirm once you have completed the setup and acknowledge the best practices.
```
